This files are contain secret main codes.

## How to run (recommended)
1. Activate Python environment:
   ```bash
   python -m venv venv
   # Windows PowerShell
   .\venv\Scripts\Activate.ps1
   pip install -r requirements.txt

run codes in VScode or jupyter notebook.    