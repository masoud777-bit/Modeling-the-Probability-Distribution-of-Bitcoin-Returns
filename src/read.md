This folder contains the main implementation code.

## How to run (recommended)
1. Activate Python environment:
   ```bash
   python -m venv venv
   # Windows PowerShell
   .\venv\Scripts\Activate.ps1
   pip install -r requirements.txt

run codes in VScode or jupyter notebook.    
